# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment
.venv/
.env
# NOTE: uv.lock IS committed (reproducible installs) — do not ignore it.

# Training artifacts — adapters live on the HF Hub, not in git
outputs/
results/adapters/
wandb/

# Ephemeral partial-run files from a crashed eval; cleaned up on success.
results/*.partial.json
results/*.partial.json.tmp

# Local model / dataset cache
.cache/
hf_cache/

# Notebooks
notebooks/scratch/
.ipynb_checkpoints/

# mkdocs build output (the deployable site is built fresh in CI on every push)
site/

# OS / editor
.DS_Store
